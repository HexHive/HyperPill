# A checklist

## Can ignore
          conveyor.cc: arch-agnostic, libfuzze's input consumer
           conveyor.h: arch-agnostic
                db.cc: arch-agnostic, to store PIO/MMIO regions

          link_map.cc: arch-agnostic, add pc ranges
               cov.cc: arch-agnostic, emulators's callbacks update coverage
         sourcecov.cc: arch-agnostic, source-based code coverage
    sym2addr_linux.cc: arch-agnostic, symbolization
	 symbolize.cc: arch-agnostic, symbolization
              enum.cc: arch-agnostic, input space enumeration
          feedback.cc: arch-agnostic

## Have to reimplement (marked as TODO)
              fuzz.cc: arch-specific
              main.cc: arch-specific
                arch/: arch-specific
